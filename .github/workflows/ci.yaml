name: CI

on:
  push:
    paths-ignore:
      - '**.md'
    branches: 
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: run
        uses: DurandMathieu/dummy-github-action@v4